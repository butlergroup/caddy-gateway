apiVersion: v1
kind: Service
metadata:
  name: {{ include "caddy-gateway.fullname" . }}
  labels:
    {{- include "caddy-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "caddy-gateway.selectorLabels" . | nindent 4 }}
